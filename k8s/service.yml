apiVersion: v1
kind: Service
metadata:
  name: okudyr
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "okudyr"
    port: 8080
  selector:
    app: okudyr
