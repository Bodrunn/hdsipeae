apiVersion: v1
kind: Service
metadata:
  name: okudyrds
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "okudyrds"
    port: 8080
  selector:
    app: okudyrds
