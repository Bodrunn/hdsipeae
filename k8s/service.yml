apiVersion: v1
kind: Service
metadata:
  name: hdsipeaea
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hdsipeaea"
    port: 8080
  selector:
    app: ohdsipeaea
