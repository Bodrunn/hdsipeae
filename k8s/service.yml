apiVersion: v1
kind: Service
metadata:
  name: hdsipeae
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hdsipeae"
    port: 8080
  selector:
    app: ohdsipeae
